import React from 'react';
// import './Spinner.css'; Assuming the CSS is defined in a separate file

const Spinner = () => {
    return (
        <div className="spinner-container">
          <div className="spinner-brain"></div>
        </div>
      );
};

export default Spinner;